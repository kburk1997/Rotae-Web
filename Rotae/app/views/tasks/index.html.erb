<h1>Your Tasks:</h1>
 <%= link_to 'New task', new_task_path %>
<table>
  <tr>
    <th>Title</th>
    <th>Estimated time(minutes)</th>
  </tr>
 
  <% @tasks.each do |task| %>
    <tr>
      <td><%= task.task_name %></td>
      <td><%= task.task_time %></td>
    </tr>
  <% end %>
</table>